// ROW_NUMBER === maximum guesses by user before game ends
export const ROW_NUMBER = 8;

// length of code (guess)
export const SOCKET_NUMBER = 4;

// Array of strings instead of numbers to avoid stringifying ids in the future
export const OPTIONS = ['0', '1', '2', '3', '4', '5'];
